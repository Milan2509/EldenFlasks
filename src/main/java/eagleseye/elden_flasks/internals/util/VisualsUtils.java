package eagleseye.elden_flasks.internals.util;

import net.minecraft.entity.LivingEntity;
import net.minecraft.entity.player.PlayerEntity;

public class VisualsUtils {
    public static void sendHealingVisuals(LivingEntity user){
        if(user instanceof PlayerEntity player){
//            player.playSound(SoundEvents);
        }
    }

    public static void sendRechargeVisuals(LivingEntity user){
        if(user instanceof PlayerEntity player){
//            player.playSound(SoundEvents);
        }
    }

    public static void sendEnhanceVisuals(LivingEntity user){
        if(user instanceof PlayerEntity player){
//            player.playSound(SoundEvents);
        }
    }
}
